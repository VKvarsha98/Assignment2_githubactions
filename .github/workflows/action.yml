

name: Github actions

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.ASSIGNMENT2 }}" | docker login -u "varshavk98" --password-stdin
        docker image build -t varshavk98/Githubactions_assignment:latest .
        docker push uses: docker/build-push:latest
